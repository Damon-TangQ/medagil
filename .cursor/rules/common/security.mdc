---
description: 安全规范（密钥、输入、鉴权、敏感数据）
alwaysApply: true
---

# 安全规范

## 密钥与配置

- 禁止在代码中硬编码 API Key、密码、Token；使用环境变量或安全配置中心。
- `.env*` 不提交；生产密钥仅放在部署环境，不写入仓库与 git 历史。

## 输入与输出

- 所有用户输入与外部数据必须校验（Zod/服务端 schema）；禁止未校验直接拼 SQL 或执行命令。
- 输出时对用户生成内容做转义，防止 XSS；敏感接口按需限流。

## 鉴权与权限

- 用户端与管理端路径与鉴权分离：`/api/v1/*` 与 `/api/v1/admin/*`，各自校验身份与权限（RBAC 在 admin）。
- 敏感操作（支付、改密、删数据）需二次确认或权限校验，并记录审计日志。

## 敏感数据

- 不在日志、错误信息中输出密码、Token、完整卡号；生产环境关闭敏感调试信息。
