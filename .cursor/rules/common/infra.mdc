---
description: 运维与部署规范（Docker、网关、监控、密钥）
globs: "infra/**/*,**/Dockerfile*,**/docker-compose*.yml,**/.env.example"
alwaysApply: false
---

# 运维与部署规范

## 目录与职责

- **infra/**：基础设施与运维配置，详见 [infra/README.md](mdc:infra/README.md)。
- **infra/docker/**：Docker Compose 编排（`docker-compose.yml` 开发、`docker-compose.prod.yml` 生产）。
- **infra/gateway/**：APISIX 网关配置（路由、鉴权、限流、日志）。
- **infra/monitoring/**：Prometheus、Grafana 等监控与日志配置。
- 与 [project/docs/技术实现方案(MVP).md](mdc:project/docs/技术实现方案(MVP).md) 第 6 节对齐；MVP 阶段可预留占位。

## Compose 组件

| 组件 | 用途 |
|------|------|
| PostgreSQL | 用户、权限、订单、订阅、积分、系统配置 |
| MongoDB | 项目、任务/对话、成果库 |
| Redis | 会话、限流、缓存、任务队列 |
| api-service | Go 主业务 |
| ai-service | Python 知识库与 AI（uv 管理，`uv sync --frozen` 安装） |
| web、admin | Next.js 前端 |
| APISIX | 网关 |

## 配置与密钥

- **禁止**在镜像或 Compose 中硬编码 API Key、密码、Token；通过环境变量或 Docker secrets 注入。
- 提供 `.env.example` 模板（不含真实密钥）；`.env` 不提交。
- 开发/测试/生产配置分开管理。
