---
description: 可访问性与国际化规范
alwaysApply: false
globs: "apps/**/*.{ts,tsx}"
---

# 可访问性与国际化

## 可访问性（a11y）

- 交互元素可键盘聚焦；焦点顺序合理。
- 图片提供替代文本（alt）；图标按钮提供 aria-label 或可见文案。
- 使用 Radix UI 时保留默认 a11y 行为，不随意移除 role/aria 属性。
- 色彩对比符合基本可读性；不单独用颜色表达信息。

## 国际化（i18n）

- 用户可见文案统一通过文案 key 或 i18n 方案管理，不硬编码多语言字符串。
- 日期、数字、货币按 locale 格式化；与产品约定一致（如先支持中文，再扩展英文）。
