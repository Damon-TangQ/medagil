# Medagil 管理端

Next.js 14+、TypeScript、Radix UI、Tailwind CSS、Zustand、React Query。

## 功能模块

- 数据看板、用户管理、任务与对话管理、项目与成果
- 会员与订单、积分、知识库管理、内容与运营、系统设置

## 运行

```bash
pnpm install
pnpm dev
```

默认 `http://localhost:3001`（与用户端 3000 区分）。
